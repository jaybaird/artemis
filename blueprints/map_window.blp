using Gtk 4.0;
using Adw 1;

template $MapWindow : Adw.ApplicationWindow {
  width-request: 800;
  height-request: 600;
  title: _("Map");

  [titlebar]
  Adw.HeaderBar {
    
  }

  Overlay {
    
  }
}